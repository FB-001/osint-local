[project]
name = "osint-local"
version = "0.1.0"
description = "Ferramenta local, modular e explicável para apoio à análise OSINT"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
osint-local = "osint_local:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
